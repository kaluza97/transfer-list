export interface DefaultTheme {
  borderRadius: string;
  fonts: {
    main: string;
    secondary: string;
  };
  fontSizes: {
    small: string;
    medium: string;
    large: string;
    xl: string;
  };
}

export interface ColorsInterface {
  main: string;
  secondary: string;
  white: string;
  black: string;
}
